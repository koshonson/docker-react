language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t hsrokosatka/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true hsrokosatka/docker-react npm run test
